apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: training-management-commands-claim0
  name: training-management-commands-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
