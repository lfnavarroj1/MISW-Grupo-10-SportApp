apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.32.0 (HEAD)
  labels:
    io.kompose.service: training-management-commands
  name: training-management-commands
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: training-management-commands
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.32.0 (HEAD)
      labels:
        io.kompose.network/backend-em-net: "true"
        io.kompose.service: training-management-commands
    spec:
      containers:
        - env:
            - name: DATABASE_URL
              value: postgresql://postgres:tmCommands@db_training_management_commands:5432/db_training_management_commands
            - name: GOOGLE_APPLICATION_CREDENTIALS
              value: /secrets/service-account.json
          image: training-management-commands
          name: training-management-commands-container
          ports:
            - containerPort: 3004
              hostPort: 3004
              protocol: TCP
          volumeMounts:
            - mountPath: /secrets/service-account.json
              name: training-management-commands-claim0
      restartPolicy: Always
      volumes:
        - name: training-management-commands-claim0
          persistentVolumeClaim:
            claimName: training-management-commands-claim0
