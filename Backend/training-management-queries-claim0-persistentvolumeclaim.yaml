apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: training-management-queries-claim0
  name: training-management-queries-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
