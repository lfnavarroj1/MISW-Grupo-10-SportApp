apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.32.0 (HEAD)
  labels:
    io.kompose.service: db-event-management-commands
  name: db-event-management-commands
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: db-event-management-commands
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert
        kompose.version: 1.32.0 (HEAD)
      labels:
        io.kompose.network/backend-em-net: "true"
        io.kompose.service: db-event-management-commands
    spec:
      containers:
        - env:
            - name: POSTGRES_DB
              value: db_event_management_commands
            - name: POSTGRES_PASSWORD
              value: emCommands
            - name: POSTGRES_USER
              value: postgres
          image: postgres:13
          livenessProbe:
            exec:
              command:
                - pg_isready -U postgres -d db_event_management_commands
            failureThreshold: 5
            periodSeconds: 5
            timeoutSeconds: 10
          name: db-em-commands
      restartPolicy: Always
