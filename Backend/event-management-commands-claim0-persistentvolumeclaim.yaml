apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: event-management-commands-claim0
  name: event-management-commands-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
